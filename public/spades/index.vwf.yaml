---
extends: http://vwf.example.com/navscene.vwf
properties:
  navmode: none
methods:
  buildDeck:
  clientCreated:
  removePlayer:
  newClientMedia:
  playerJoined:
scripts:
- |
    this.initialize = function() {
      console.info( "=========== spades ============="  );
      //createDeck();
      //this.future(30).buildDeck();
    } 
    this.clientCreated = function( moniker ) {

    }
    this.playerJoined = function( moniker ) {
      
    }
    this.removePlayer = function( moniker ) {
      
    }
    this.newClientMedia = function( moniker ) {
      
    }    
    this.buildDeck = function() {
      createDeck();
    } //@ sourceURL = card.vwf

