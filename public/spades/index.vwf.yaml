---
extends: http://vwf.example.com/navscene.vwf
properties:
  navmode: none
methods:
  buildDeck:
scripts:
- |
    this.initialize = function() {
      console.info( "=========== spades ============="  );
      //createDeck();
      //this.future(30).buildDeck();
    } 
    this.buildDeck = function() {
      createDeck();
    } //@ sourceURL = card.vwf

